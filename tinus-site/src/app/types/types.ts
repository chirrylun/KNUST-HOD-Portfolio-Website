export type Publication = {
    authors: string
    title: string
    description: string
    link: string
  }
  
  export type Event = {
    title: string
    subtitle: string
    description: string
    link: string
  }